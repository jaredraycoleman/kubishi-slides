{
  "name": "kubishi-slides",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy ./dist --project-name=kubishi-slides",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.9",
    "marked": "^18.0.2"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
